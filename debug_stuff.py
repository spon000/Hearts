# import logging
# from functools import wraps

# def set_global_log_level(level = logging.ERROR):
#     def set_log_level(in_function):
#         def wrapper(*args, **kwargs):
#             logging.basicConfig(level = level)


# def set_local_log_level(level = )

# def log_function_start(in_function):
#     import logging
#     logging.basicConfig(filename="log.txt", filemode="w", encoding = "utf 8", format = "%(name)s - %(levelname)s - %(message)s")

#     @wraps(in_function)
#     def wrapper(*args, **kwargs):

